FROM node:20-alpine

WORKDIR /app

# Enable pnpm
RUN corepack enable pnpm

# Copy package.json
COPY package.json ./

# Install dependencies 
RUN pnpm install

# Expose port
EXPOSE 3000

# Development command with PATH adjustment
CMD ["sh", "-c", "pnpm install && pnpm dev"]